{
  "name": "gafer",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "pretest": "pnpm --filter @gafer/contracts build && pnpm --filter @gafer/api build",
    "build": "pnpm -r --if-present build",
    "test": "pnpm -r --if-present test",
    "lint": "pnpm -r --if-present lint"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  },
  "packageManager": "pnpm@11.20.0"
}
